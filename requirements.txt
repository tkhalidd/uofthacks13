# Core 3D Reconstruction
# NOTE: We're using SAM 3D for everything now (room + furniture)
# SAM 3D runs on Vultr GPU server, not locally
# torch>=2.0.0  # Only needed if running models locally
# torchvision>=0.15.0

# 3D Processing
open3d>=0.18.0
trimesh>=4.0.0
pymeshlab>=2023.12

# Computer Vision & ML
opencv-python>=4.8.0
ultralytics>=8.0.0  # YOLO-v8
numpy>=1.24.0
scipy>=1.11.0
scikit-image>=0.21.0

# API & Backend
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Data Processing
pillow>=10.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Optimization
cvxpy>=1.4.0  # Convex optimization
networkx>=3.1  # Graph-based layout optimization

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Optional: Semantic Segmentation
# transformers>=4.35.0  # For CLIP or other models
# segment-anything>=1.0  # Meta's SAM (install with: pip install git+https://github.com/facebookresearch/segment-anything.git)

# Gemini AI Integration
google-generativeai>=0.3.0

# Photo to 3D Generation
# Note: Install specific backend you want to use:
# pip install trellis-3d  # For TRELLIS.2 (recommended)
# OR install SAM 3D from: https://github.com/facebookresearch/sam-3d-objects
# OR install TripoSR from: https://github.com/VAST-AI-Research/TripoSR

# Optional: Advanced features
# pycolmap  # Python bindings for COLMAP
# pytorch3d  # Advanced 3D operations

